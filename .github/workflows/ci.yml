# Thin caller — delegates to the org-wide reusable CI workflow.
# See: https://github.com/teqbench/.github/.github/workflows/ci.yml

name: TeqBench Package - CI Workflow

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: ci-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    uses: teqbench/.github/.github/workflows/ci.yml@5b246088833509a300b4b6d1e3700a3e5cfc69c4 # main
    with:
      gist-id: ${{ vars.GIST_ID }}
    secrets: inherit
